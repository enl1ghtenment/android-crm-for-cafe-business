<resources>
    <style name="Base.Theme.CRM" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorOnPrimary">@color/md_on_primary</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorOnSecondary">@color/md_on_secondary</item>
        <item name="android:colorBackground">@color/md_bg</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@android:color/white</item>
        <item name="colorOutline">@color/md_outline</item>

        <item name="colorOnSurfaceSecondary">#E0FFFFFF</item>


    <!-- Глобальные виджеты -->
        <item name="materialButtonStyle">@style/Btn.Filled</item>
        <item name="materialButtonOutlinedStyle">@style/Btn.Outlined</item>

        <!-- Базовые стили полей для экранов -->
        <item name="textInputStyle">@style/TextInput.Outlined</item>
        <item name="editTextStyle">@style/EditText.CRM</item>

        <!-- Тема диалогов -->
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.CRM.Dialog</item>

        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.CRM" parent="Base.Theme.CRM"/>

    <!-- Кнопки -->
    <style name="Btn.Filled" parent="Widget.Material3.Button">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Btn.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Поля ввода для экранов (ночь) -->
    <style name="TextInput.Outlined" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="hintTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

    <style name="EditText.CRM" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

    <!-- ===== СТИЛИ ДЛЯ ДИАЛОГОВ (ночь) ===== -->
    <style name="TextInput.Outlined.Dialog" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundMode">outline</item>
        <item name="boxBackgroundColor">@android:color/transparent</item>
        <item name="boxStrokeColor">@color/md_outline</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="hintTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
    </style>

    <style name="EditText.CRM.Dialog" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:background">@null</item>
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <style name="ThemeOverlay.CRM.Dialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <!-- Ночная палитра диалога -->
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@android:color/white</item>

        <!-- Подключаем диалоговые варианты инпутов -->
        <item name="textInputStyle">@style/TextInput.Outlined.Dialog</item>
        <item name="editTextStyle">@style/EditText.CRM.Dialog</item>

        <!-- Текстовые стили диалога -->
        <item name="textAppearanceBodyMedium">@style/Dialog.Text</item>
        <item name="textAppearanceHeadlineSmall">@style/Dialog.Title</item>
    </style>

    <style name="Dialog.Text" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Dialog.Title" parent="TextAppearance.Material3.HeadlineSmall">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Прочее -->
    <style name="Shape.Rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>
    <style name="Shape" parent="Shape.Rounded" />

    <style name="Btn.Tonal" parent="Widget.Material3.Button.TonalButton">
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
